export function NavigationDescription() {
  return (
    <div className="text-muted-foreground text-center text-xs italic">
      (Use arrow keys / click on sides / swipe to navigate pages. Swiping only
      works when zoomed out.)
    </div>
  );
}
